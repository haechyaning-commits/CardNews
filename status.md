# 진행 상황 (status.md)

이 파일은 "오늘 어디까지 했는지 / 내일 뭐 할지"를 매일 갱신하는 작업 로그입니다.
git 커밋 히스토리로도 추적되지만, 커밋 메시지만으로는 "왜 이렇게 바꿨는지"가
잘 안 보여서 별도로 남겨둡니다.

## 2026-09-07

### 오늘 한 일

**작가(Writer) + PM 에이전트 + 오케스트레이터 — 반려/통과 루프 구축**

- `writer_agent.py`: skill 문서(`skill_beauty_skincare.md`)의 정확성 원칙/작가
  규칙을 읽어서 시스템 프롬프트에 반영하고, tool-use로 출력 형식(JSON 스키마)을
  강제해서 대본을 생성. PM에게 반려당하면 피드백을 받아 다시 쓰는 재작성 경로도
  지원.
- `pm_agent.py`: 검수를 2단계로 분리.
  1) 기계적 체크(API 호출 없이 코드로 즉시 판단) — 슬라이드 수(7~10장),
     면책 문구 포함 여부, 금지된 단정적 표현("무조건", "100% 효과" 등),
     슬라이드별 이모티콘/강조부호 포함 여부.
  2) 판단 체크(Claude 호출) — 의학적 근거의 타당성, 완독 유도 장치 충분성,
     오타/문맥에 안 맞는 단어 여부.
  구조 검증(`validate_structure`)을 다른 체크보다 먼저 돌려서, tool-use
  스키마가 가끔 깨지는 경우(예: slides 항목이 객체가 아니라 문자열로 옴)에
  다른 체크에서 `.get()` 호출로 죽지 않고 바로 반려 처리되게 함.
- `shared_rules.py`: 금지 표현/이모티콘 규칙을 작가와 PM이 각자 다른 곳에서
  들고 있어서 기준이 어긋나는 문제(PM 기준과 작가가 실제로 아는 규칙이 서로
  다름)가 있었음. 상수를 한 파일로 모아 양쪽이 같은 목록을 보게 정리.
- `orchestrator.py`: 작가→PM→(반려 시)작가 재작성 루프를 최대 3회까지 실행.
  전체 시도 기록(대본+PM 피드백)을 `review_log.json`에 남겨서 "몇 번 반려
  끝에 통과했는지"를 포트폴리오에서 그대로 보여줄 수 있게 함.

**안정성 테스트 (`stability_test.py`)**

주제 5개를 오케스트레이터로 연속 실행해서 반려/통과 루프가 실제로 얼마나
안정적인지 확인함 (`stability_report.json`).

- 5개 주제 중 4개 최종 승인, 1개("지성 피부 유수분 밸런스")는 3회 시도 후에도
  통과 못 하고 `max_revisions_reached`로 종료.
- 가장 자주 걸린 반려 항목은 압도적으로 "슬라이드별 이모티콘/강조부호"
  누락 — 5개 주제 전부 1차 시도에서 이 항목으로 걸림. 작가 에이전트가
  이 규칙을 시스템 프롬프트에서 보긴 하지만 첫 시도에서 잘 안 지키고,
  PM 피드백을 받은 다음에야 반영하는 패턴이 뚜렷함.
- "지성 피부 유수분 밸런스" 주제는 단정적 표현("무조건" 등)과 이모티콘
  누락이 반복해서 걸려 3회 안에 못 고쳤음 → 내일 원인 확인 필요.

**사진수집가(Photo Collector) 에이전트 — `photo_agent.py` 대규모 개선**

- 소스 확장: Unsplash 단일 소스에서 Pexels를 추가한 2-소스 구조로 변경.
  Unsplash만 쓰면 서구권 모델/사진작가 위주로 후보가 쏠려서 "한국 20대
  라이프스타일" 느낌을 찾기 어려웠던 문제를 완화하기 위함.
- 톤 우선 설계(tone-first): 카드 하나하나 사진을 고른 뒤 나중에 밝기를
  맞추는 방식(선택 후 보정)에서, 대본 전체를 보고 세트 전체 톤을 먼저 정한
  뒤 그 톤에 맞는 사진만 검색·선택하는 방식(`decide_tone()`)으로 구조 변경.
- 비전 기반 선택으로 전환: `pick_best()`가 후보 사진의 alt_description
  텍스트만 보고 판단하던 것을, 실제 썸네일 이미지를 Claude에게 보여주고
  판단하게 바꿈. 텍스트만 볼 때 "종이 질감"을 "스킨케어와 은유적으로
  어울린다"고 억지로 갖다 붙이는 문제가 있었음.
- `texture_detail` 구도 제거: 위 문제의 근본 원인 중 하나. 실물이 명확한
  구도(얼굴/상반신/손동작/제품/일상)만 남김.
- 표지(role="표지") 전용 임팩트 판단 로직, 카드 내용과 무관한 후보뿐일 때
  `no_good_match`로 거절할 수 있는 로직, 재시도 라운드에서는 무조건 하나
  고르게 하는 `allow_reject=False` 안전장치 추가.
- Unsplash 403 레이트리밋(데모 앱 시간당 50요청) 대응: 한 번 403을 만나면
  `_unsplash_rate_limited` 플래그를 세워 이번 실행 내내 Pexels로만 진행하도록
  graceful degradation 처리. 이전엔 카드 8 처리 중 크래시났음.
- 연속 인물샷 방지: "웬만하면 다른 구도로" 같은 프롬프트 지시로는 서로 다른
  구도(얼굴 클로즈업→상반신→일상장면)가 전부 인물 사진이라 4장 연속 인물샷이
  나오는 걸 못 막았음. `MAX_CONSECUTIVE_PERSON_SHOTS=2`로 카운터를 두고,
  기준을 넘으면 다음 카드는 `QUERY_TOOL`의 shot_type 선택지 자체를 인물 없는
  구도로만 코드로 강제하도록 변경(프롬프트 지시 → 선택지 제한).
- 영구 차단 목록(`banned_photos.json` + `ban_photo.py`): 특정 사진작가/사진이
  세트를 새로 만들 때마다 계속 나오는 문제 대응. `python3 ban_photo.py
  photographer "이름"` 으로 한 번 등록하면 이후 모든 실행에서 후보 풀
  자체에서 제외됨. list/unban 명령도 지원.
- 자동 품질 랭킹(좋아요 수 기반): "매번 사람이 보고 별로인 사진을 수동으로
  차단해야 하는 건 완전 자동화가 아니다"라는 피드백을 받고, Unsplash가
  제공하는 좋아요 수를 랭킹에 반영 — 좋아요 50개 미만인 무명 사진은 자동
  감점되어 애초에 후보 상위권에 덜 올라오게 함. Pexels는 좋아요 수를 API로
  안 줘서 중립 처리. 터미널 로그와 `preview.html` 양쪽에 실제 좋아요 수를
  표시해서 랭킹이 실제로 반영됐는지 눈으로 확인 가능하게 함.

**후처리(`crop_images.py`) 개선**

- 정확히 4:5(1080x1350) 크롭은 기존과 동일.
- 2단계 톤 보정 추가: (1) 세트 평균 밝기를 목표로 밝기를 통일하는
  `normalize_tone()`, (2) 계정 고유 웜+뮤트 필터 `apply_brand_filter()`.
  웜 필터 수치는 "톤이 너무 누렇다" 피드백을 받고 원래 값의 절반 이하로 낮춤.

**의사결정 사항**

- 특정 사진 속 인물의 외모를 "못생겼다"는 기준으로 걸러달라는 요청은 구현하지
  않음. 대신 그 요청의 정당한 의도(더 전문적/화보st 사진 선호)만 살려서
  검색어와 `pick_best()` 판단 기준에 "조명·스타일링·연출 완성도" 기준으로
  반영함 — 실존 인물의 외모 평가 로직은 만들지 않는다는 원칙을 지킴.

### 검증 방법

- 작가+PM 루프: `stability_test.py`로 실제 API를 호출해서 5개 주제 x 최대
  3회 재시도를 통으로 돌려 반려/통과 패턴을 `stability_report.json`으로 남김.
- 사진수집가: API 키가 이 작업 환경(샌드박스)엔 없어서, `unittest.mock`으로
  Unsplash/Pexels 응답과 Claude 응답을 흉내 낸 목(mock) 테스트로 검증함 —
  영구 차단 필터링, 연속 인물샷 카운터, 좋아요 기반 랭킹, `ban_photo.py`
  CLI 동작을 각각 확인.

### 내일 할 일

1. **"슬라이드별 이모티콘/강조부호" 반복 반려 원인 개선** — 5개 주제 전부
   1차 시도에서 이 항목으로 걸렸음. 작가 프롬프트에서 이 규칙의 위치/강조
   방식을 바꿔서 첫 시도부터 통과율을 올릴 수 있는지 확인.
2. **"지성 피부 유수분 밸런스" 주제가 3회 반려 끝에도 통과 못 한 원인 분석**
   — 단정적 표현이 반복해서 나온 이유(주제 특성상 유수분 밸런스는 "확실한
   효과"를 강조하고 싶어지는 주제라 그런 건지)를 review_log.json으로 확인.
3. **디자이너(Designer) 에이전트 구현** — 지금까지는 대본 승인 + 사진 선택/
   크롭/톤 보정까지 끝났고, 실제로 카드 위에 문구(표지 hook, 슬라이드 text)를
   합성해서 인스타그램에 올릴 최종 이미지를 만드는 단계가 아직 없음. PIL로
   텍스트 오버레이하는 방식으로 시작.
4. **README.md 작성** — 전체 파이프라인(작가→PM→사진수집가→크롭/톤보정→
   디자이너) 구조, 설치/실행 방법, 각 에이전트가 무슨 문제를 어떻게
   해결했는지(포트폴리오 관점에서 "왜 이렇게 설계했는지"가 핵심) 정리.
5. **GitHub에 노출됐던 PAT(Personal Access Token) 재발급/폐기 여부 재확인**
   — 이전에 실수로 노출된 적이 있었는데 확실히 폐기했는지 다시 확인 필요.
6. 전체 파이프라인 처음부터 끝까지(주제 입력 → 최종 이미지) 한 번 통으로
   돌려서 엔드투엔드로 문제없는지 확인.

### 참고

- 리포지토리: https://github.com/haechyaning-commits/CardNews
- `Claude outputs/photo_agent.py` 라는 폴더가 프로젝트 폴더 안에 남아있는데,
  예전에 실수로 잘못된 경로에 저장된 잔여 파일임. `.gitignore`에는 추가해뒀지만
  Finder에서 폴더째 지워도 무방함.

## 2026-09-08

### 오늘 한 일

**디자이너(Designer) 에이전트 신설 — `designer_agent.py`**

- 파이프라인의 마지막 빈 구간(대본 승인 + 사진 배정/크롭/톤보정까지는 되지만
  실제 발행용 최종 이미지가 없던 부분)을 채움. `crop_manifest.json`(카드별
  1080x1350 로컬 이미지) + `approved_script.json`(카피)을 합쳐서 문구를 얹은
  PNG를 만든다. API 호출 없이 로컬에서 PIL만으로 동작.
- Skill 문서 3번(디자이너 규칙)을 코드로 강제: 템플릿 3종(헤더/본문/마무리)
  고정, 폰트 최대 2종(제목=볼드/본문=레귤러), 상하 15% 세이프존 안에 텍스트
  배치, 사진 위 텍스트는 항상 어두운 스크림 뒤에 놓아 가독성 확보.
- `approved_script.json`의 `cta`/`comment_question`은 어느 슬라이드에도 안
  묶여 있어서(표지+슬라이드만 8장), 마지막 슬라이드 이미지를 재사용해 "마무리"
  카드를 하나 더 만들어 CTA+댓글유도질문+계정 핸들을 얹음 — 총 9장이 되고
  Skill의 "7~10장" 범위 안에 그대로 들어감.
- 스크림(어두운 그라데이션) 버그 하나 잡음: 처음엔 이미지 하단 고정 비율(예:
  50%)부터 스크림을 깔았는데, 슬라이드마다 문구 길이가 달라서 문구가 긴
  카드는 첫 줄이 스크림이 옅은 지점에 걸려 밝은 사진에서 가독성이 떨어지는
  문제가 있었음. 텍스트를 먼저 줄바꿈 계산해서 블록 높이/시작 y좌표를 구한
  다음, 그 지점을 기준으로 스크림을 까는 순서로 바꿔서 문구 길이와 무관하게
  텍스트 전체가 항상 짙은 스크림 위에 놓이게 함.
- 상단 역할 태그/페이지 인디케이터는 하단 스크림이 안 닿는 영역이라 사진이
  아주 밝으면(흰 배경 등) 여전히 묻힐 수 있어서, 텍스트마다 작은 반투명 박스를
  뒤에 깔아 사진 밝기와 무관하게 항상 읽히도록 보강.
- `**키워드**` 마크업 지원 미리 만들어둠(포인트 색상 강조) — Skill 3번이
  요구하는 기능이지만 `writer_agent.py`가 아직 이 마크업을 안 써서 지금은
  전부 기본 스타일로만 렌더링됨. 나중에 작가 에이전트가 대본에 `**단어**`를
  넣기 시작하면 디자이너 쪽 코드 변경 없이 바로 강조 처리됨.
- 한글 폰트 자동 탐색: 프로젝트 `fonts/` 폴더 → 흔한 OS별 한글 폰트(맑은 고딕,
  Apple SD 산돌고딕, 나눔고딕, Noto Sans CJK) 순으로 찾고, 하나도 없으면
  Noto Sans KR을 받아 `fonts/`에 넣으라고 안내하고 멈춤. `FONT_REGULAR_PATH`/
  `FONT_BOLD_PATH` 환경변수로 직접 지정도 가능.

### 검증 방법

- API 키가 없는 이 작업 환경에서 실제 사진수집가/크롭 단계를 돌릴 수 없어서,
  `approved_script.json`의 실제 카피를 그대로 쓰되 이미지는 단색으로 채운
  가짜 `crop_manifest.json` + `images/`를 만들어 `designer_agent.py`를
  통으로 실행해 검증함(정식 테스트 파일로 커밋하진 않음, 로컬 확인용).
- 스크림 가독성은 최악의 경우(거의 흰색 배경)로도 한 번 더 돌려서, 문구가 가장
  긴 카드까지 전부 텍스트가 짙은 영역 안에 들어오는지 눈으로 확인.
- 한글 렌더링은 이 환경에 없던 `fonts-noto-cjk`를 설치해서 실제 폰트로 확인함
  (사용자 macOS 환경엔 이미 한글 폰트가 있어서 별도 설치 불필요할 가능성 높음).

**실제 사진으로 디자이너 에이전트 반복 개선**

사용자가 본인 macOS 환경에서 실제 API 키로 파이프라인을 돌려서 진짜 사진 위
렌더링 결과를 여러 차례 확인 → 피드백 → 수정하는 루프를 반복함.

- **마무리 카드 사진 중복 버그 수정**: `approved_script.json`의 `cta`/
  `comment_question`이 어느 사진에도 안 묶여 있어서 마지막 슬라이드 사진을
  재사용했는데, 그러면 캐러셀 마지막 두 장이 똑같은 사진으로 겹쳐 보임.
  `photo_agent.py`가 마무리 카드 전용 사진도 따로 검색·배정하도록 수정하고,
  전용 사진이 없는 예전 `crop_manifest.json`을 쓸 때는 재사용 폴백으로 자동
  전환(콘솔에 안내 출력)해서 파이프라인이 죽지 않게 함.
- **디자인 대개편(표지/역할태그/박스 스타일)**: 상단 역할 태그·페이지 번호
  제거, 표지 제목을 짧고 임팩트 있게(킥커 문구 + 동적 폰트 크기 + 핵심
  단어 강조), 박스 배색(흰색/어두운색)은 세트 전체에서 한 번만 정해 통일하되
  좌/우·상/중 위치는 카드마다 자유롭게(사진 수집가가 세트 톤을 한 번만
  정하는 것과 같은 원리), 박스 테두리 선 제거.
- **스크림(어두운 그라데이션) 완전 제거 → 다시 되돌림**: "글씨 뒤에 검은
  배경 같은 게 이상하다"는 피드백으로 스크림을 없애고 표지/헤딩/포인트/
  마무리는 불투명 박스, 본문 문단은 박스 없이 흰 글자+어두운 외곽선(stroke)
  으로 바꾸는 v5를 구현·검증·푸시까지 했으나, 사용자가 실제 결과를 보고
  이전 버전(스크림 있는 버전)으로 되돌려달라고 해서 해당 커밋을 `git revert`
  로 되돌리고 다시 푸시함. **현재 main에 병합되는 시점 기준으로 스크림 있는
  버전이 최종 상태.**
- **한글 텍스트 강조 경계 버그 수정**: `**클렌저**로`처럼 볼드 명사에 조사가
  붙어 띄어쓰기 없이 이어지는 경우, 강조 구간과 일반 구간 사이에 공백이
  잘못 삽입되던 문제를 토큰을 (조각, 강조여부) 단위로 재설계해서 수정.
- **이모지 번호 깨짐("1□□") 버그 수정**: Claude가 번호를 매기려고 1️⃣2️⃣3️⃣
  같은 이모지를 쓰면 한글 폰트에 그 글리프가 없어 네모로 깨짐 —
  `_strip_unrenderable()`로 문제 유니코드 대역을 걸러내고 프롬프트에도
  "1." 같은 일반 숫자를 쓰라고 안내.
- **볼드 강조 미반영 버그 수정**: `annotate_cards()`의 안전장치(Claude가
  `**`를 뺀 본문이 원문과 정확히 일치하는지 검사)가 공백 차이만으로도 전체
  강조를 무효화하던 문제를, 공백을 정규화해서 비교하도록 완화.

### 내일 할 일

1. **실제 파이프라인으로 한 번 더 통 검증** — 오늘 반복한 수정사항(마무리
   카드 전용 사진, 강조 경계, 이모지 번호)이 실제 API 키 환경에서 여러
   주제로도 안정적인지 다시 한번 end-to-end로 확인.
2. **"본문 박스 없이 vs 스크림" 디자인 방향 재검토** — 오늘 v5(스크림 제거
   + 박스/외곽선 텍스트)까지 갔다가 스크림 버전으로 되돌렸음. 왜 되돌렸는지
   (실제 사진에서 어떤 부분이 별로였는지) 구체적으로 정리해서, 스크림을
   유지하면서도 개선할 지점이 있는지 다음 라운드에서 다시 짚어보기.
3. **계정 핸들 확정** — 지금 `ACCOUNT_HANDLE` 환경변수 없으면 "@계정핸들"
   자리표시자가 그대로 박힘. 실제 계정이 정해지면 값 채워넣기.
4. "슬라이드별 이모티콘/강조부호" 반복 반려 원인 개선, "지성 피부 유수분
   밸런스" 주제 3회 반려 원인 분석, README.md 작성, PAT 재발급/폐기 재확인
   — 계속 밀려온 항목들, 디자이너 에이전트가 어느 정도 안정화됐으니 이제
   순서를 당겨서 진행.

## 2026-09-09

### 오늘 한 일

**전체 파이프라인 처음부터 하나씩 재검토 — 작가/PM/오케스트레이터, 사진수집가,
크롭/톤보정, 디자이너**

이 작업 환경엔 Anthropic/Unsplash/Pexels API 키가 전혀 없어서, 각 에이전트가
실제로 외부 API를 호출하는 지점을 `unittest`식 mock(가짜 Claude 응답, 가짜
Unsplash/Pexels 응답, 가짜 이미지 다운로드)으로 대체해서 코드 경로를 실제로
실행시켜 검증했다(임시 스크립트로만 사용, 리포에는 안 남김).

- 작가→PM→오케스트레이터: 구조 검증/기계적 체크 4종/판단 체크 조합, "1차
  반려 → 피드백 반영 → 2차 통과" 루프를 실제로 돌려서 `review_log.json`/
  `approved_script.json` 기록까지 확인. 정상.
- 사진수집가: 톤 결정, 후보 랭킹(비율·밝기·좋아요), 영구 차단 목록 필터링,
  연속 인물샷 2회 제한, `no_good_match` 폴백 재시도, Unsplash 403 →
  Pexels만으로 감속 없이 전환 — 전부 의도대로 동작 확인.
- 크롭/톤보정: 가로로 넓은/세로로 긴/이미 4:5인 사진 각각 정확히
  1080x1350으로 크롭, 밝기 정규화 상하한 클램프, 이미지 없는 카드 스킵 확인.
- 디자이너: `--demo` 실제 실행(표지/본문/마무리 렌더링) + mock
  annotate_cards로 dark 박스, 표지 킥커/제목(강조 포함)/정렬, 본문
  heading/point 박스 좌우·상중 배치, 강조-원문 불일치 폴백까지 이미지로
  직접 확인. 전부 정상.
- `ban_photo.py`: add/list/unban CLI 동작 확인.

버그는 못 찾았음 — 다들 문서/주석에 적힌 대로 동작했다.

**발견한 설계상 허점 → 안전장치 추가**

`orchestrator.py`는 3회 반려 끝에도 통과 못 하면(`max_revisions_reached`) 그
미승인 마지막 시도본을 그대로 `approved_script.json`에 저장하는데, 이후
단계(`photo_agent.py`/`designer_agent.py`)는 이 파일을 무조건 "승인된 대본"
으로 믿고 이어서 진행했다 — 즉 단정적 표현 같은 반려 사유가 안 걸러진 채로
최종 이미지까지 만들어질 수 있는 상태였음. 두 파일에 `check_approval_gate()`를
추가해서, 같은 폴더 `review_log.json`의 `final_status`가 `approved`가 아니면
기본적으로 실행을 막고(`SystemExit`), 그래도 진행하려면 `--force` 플래그를
명시하게 했다. `review_log.json`이 아예 없으면(대본을 수동으로 준비한 경우 등)
확인할 수단이 없으니 그냥 통과시킨다 — 하위 호환.

### 검증 방법

- 작가/PM/오케스트레이터, 사진수집가, 크롭/톤보정, 디자이너 각각 mock 기반
  통합 테스트로 실제 코드 경로 실행(위 "오늘 한 일" 참고).
- `check_approval_gate()`는 (1) review_log.json 없음 (2) approved (3)
  max_revisions_reached+force 미지정(차단) (4) max_revisions_reached+force
  지정(경고 후 진행) (5) review_log.json이 깨진 JSON(게이트가 전체 실행을
  막지 않고 조용히 통과) 5가지 케이스를 각각 실행해서 확인. 정상 흐름(승인된
  진짜 review_log.json 기준)에서 `designer_agent.py --demo`가 조용히 그대로
  동작하는 것도 재확인.

**스크림 제거 → 글자 외곽선(stroke)으로 교체**

사용자가 본인 macOS 환경에서 실제 사진으로 파이프라인을 돌린 결과물을
보내와서 "글씨 뒤에 약간 검은 배경이 깔리는데 이것만 없애달라"는 피드백을
받음. 09-08에 스크림 제거(v5) → 사용자가 되돌려달라고 해서 revert했던
이력이 있었는데, 이번엔 "스크림만 없애고 나머지(레이아웃/박스)는 그대로"라는
더 구체적인 요청이라 최소 범위로 다시 시도.

- 표지 제목/킥커, 본문 문단, 마무리 CTA/댓글유도질문/계정핸들 — 사진 위에
  바로 얹던 텍스트에서 `add_scrim`/`add_full_scrim`(사진을 어둡게 덮던
  반투명 그라데이션/오버레이) 호출을 전부 제거.
  대신 `draw_wrapped()`에 `stroke_width`/`stroke_fill`을 추가해서 흰 글자에
  어두운 웜톤 브라운(`COLOR_STROKE`, 스크림과 같은 색) 외곽선을 둘러
  가독성을 확보(제목 4px, 본문 3px, 라벨류 2px). PIL의 `draw.text` 내장
  stroke 기능을 그대로 씀.
  요약 제목/포인트 박스(`render_box_text`)는 원래부터 스크림이 아니라 자체
  불투명 배경이라 이번 변경과 무관 — 그대로 흰/짙은 박스 유지.
- 이제 안 쓰는 `add_scrim`/`add_full_scrim`/`_band_opacity` 함수와 관련
  주석·모듈 docstring을 정리해서 죽은 코드가 안 남게 함.

### 검증 방법

- `designer_agent.py --demo`로 단색 배경 위 표지/본문/마무리 렌더링해서
  이미지로 직접 확인 — 배경에 더 이상 그라데이션이 없고, 흰 글자에 얇은
  갈색 외곽선만 둘러져 읽힘.
- 이전에 만들어둔 mock `annotate_cards` 테스트를 재실행해서, dark 박스
  스타일/heading·point 박스가 있는 카드에서도 본문 텍스트의 스크림만
  빠지고 박스는 그대로인 것을 이미지로 확인.

### 내일 할 일

이전부터 밀려온 항목 그대로: "슬라이드별 이모티콘/강조부호" 1차 시도 반복
반려 원인 개선(프롬프트 위치/강조 방식), 실제 API 키 환경에서 end-to-end
재검증(특히 오늘 바꾼 스크림 제거가 진짜 사진 위에서도 잘 읽히는지), 계정
핸들 확정, README.md 작성, PAT 재발급/폐기 재확인.
